---
description: Review the current diff against the design system. Read-only.
---

Invoke the `design-system-reviewer` subagent on the current diff (`git diff` + `git diff --staged`).

Report violations as a numbered list with `file:line — rule violated — suggested fix`. End with `No violations` if clean.

Do NOT edit any files. This is read-only.

Reference: `/Users/gustavorh/dev/pdf-editor/docs/design-system.md`.
